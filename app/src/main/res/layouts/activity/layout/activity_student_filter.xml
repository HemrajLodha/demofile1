<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg"
    android:layoutDirection="locale"
    android:textDirection="locale">

    <android.support.design.widget.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <include layout="@layout/toolbar" />

        <com.pws.pateast.widget.FontTabLayout
            android:id="@+id/tab_student"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/white"
            android:elevation="@dimen/toolbar_elevation"
            app:fontFace="app"
            app:tabGravity="fill"
            app:tabIndicatorColor="@color/colorPrimaryTeacher"
            app:tabMode="scrollable"
            app:tabSelectedTextColor="@color/colorPrimaryTeacher"
            app:tabTextColor="@color/textColor" />

    </android.support.design.widget.AppBarLayout>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <android.support.v4.view.ViewPager
            android:id="@+id/pager_student"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <com.pws.pateast.widget.ServerResponseView
            android:id="@+id/response_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone" />

        <include
            layout="@layout/progressbar"
            android:visibility="gone" />
    </RelativeLayout>

</android.support.design.widget.CoordinatorLayout>